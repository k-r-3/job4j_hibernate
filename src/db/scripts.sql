create table j_role (
                        id serial primary key,
                        name varchar(2000)
);

create table j_user (
                        id serial primary key,
                        name varchar(2000)
);

create table j_brand (
  id serial primary key,
  name varchar(2000)
);

create table j_car_type (
                         id serial primary key,
                         name varchar(2000)
);
